// Properties
card_id = noone;
card_type = noone;
state = "hand"; // "hand" or "field"
dragging = false;
hovered = false;

base_depth = depth;

// Stats will be assigned in setup_card() after creation
stats = undefined;

// For dragging
drag_offset_x = 0;
drag_offset_y = 0;

current_scale = 1;